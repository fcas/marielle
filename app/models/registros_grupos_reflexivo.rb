class RegistrosGruposReflexivo < ApplicationRecord
    # validates :cidade, presence: { message: "É necessário informar a cidade" }
    # validates :uf, presence: { message: "É necessário informar a Unidade Federativa (UF)" }
    #
    # validates_length_of :logradouro, allow_blank: false
    # validates_length_of :numero, allow_blank: false
    # validates_length_of :bairro, allow_blank: false
    # validates_length_of :uf, minimum: 2, maximum: 2, allow_blank: false
    #
    # validates_length_of :telefone, minimum: 11, maximum: 11, allow_blank: false
    # validates_length_of :nomegruporeflexivo, allow_blank: false

end
